<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.poseidon.web.dao.IndexDAO">
	<select id="freeboard" resultType="BoardDTO">
		SELECT mtno, mttitle, mno, mtip, mtread, mtcate, mtdate, mtdel
		FROM multiboard
		LIMIT 0, 10
	</select>
	
	<select id="detail" parameterType="int" resultType="BoardDTO">
		SELECT board_no, board_title, board_content, board_date
		FROM board
		WHERE board_no=#{no}
	</select>
	
	
	
</mapper>